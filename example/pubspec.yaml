name: camerapreview_example
description: Demonstrates how to use the camerapreview plugin.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  camerapreview:
    path: ../
  flutter_native_view_android:
    git:
      url: https://github.com/p-shapovalov/flutter_native_view_android

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
